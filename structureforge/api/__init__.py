"""Optional web GUI: a FastAPI backend (`structureforge.api.app`) serving the materials/recipe
libraries and a `/api/simulate` endpoint, plus a static vanilla-JS/SVG frontend
(`structureforge/api/static/`) with the process-step builder and the two-view (overview + zoom)
cross-section canvas. Requires the `api` extra (`pip install structureforge[api]`).
"""
