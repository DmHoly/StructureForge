"""The process domain: the elementary step types (deposition, etch, planarization, chemical,
lithography, resist strip) and `simulate()`, which applies a list of them to a starting
`Geometry` and returns one rendered frame per step.
"""
